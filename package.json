{
  "name": "react-buildless",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "status": "pm2 list",
    "down": "pm2 delete all",
    "stop": "pm2 stop pyhttp",
    "start": "pm2 start \"python3 server.py\" --name pyhttp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "pm2": "^5.3.1"
  }
}
